# LM Store Frontend 🎨

Built with **Next.js 15** and **Tailwind CSS**.

## 🛠️ Setup
1. **Install Packages:**
   ```bash
   npm install
2. **Environment Variable:** Create a `.env.local` file:
   ```bash
   NEXT_PUBLIC_API_URL=http://localhost:8000
3. **Run Development Mode:**
   ```bash
   npm run dev

## 🧩 Key Components

* **AdminDashboard:** Main inventory control center with split-view alerts.
* **AddProductModal:** Framer Motion powered slide-in form
* **SecureFetch:** API wrapper for handling JWT and cookies

## 💅 Styling

* **Theme:** Neo-Brutalist (High contrast, heavy borders, bold shadows).
* **Icons:** Lucide React
